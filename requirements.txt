# Pinned to the stack actually running on wp.foxugly.com (box `pip freeze`,
# Django 6 / Python 3.12) — the previous unpinned list was stale fiction
# (claimed Django>=4.2.1, selenium 3.14, pytz 2018.5; none of which the box
# runs or the code imports). Transitives (asgiref, sqlparse, pydantic, segno,
# six, …) are resolved by pip from these direct deps.

Django==6.0.6
gunicorn==26.0.0

# UI / app
django-bootstrap-modal-forms==3.0.5
django-bootstrap4==26.1
django-widget-tweaks==1.4.2
django-hijack==3.7.8
django-hijack-admin==2.1.10
django-compat==1.0.15
django-qr-code==4.2.0
django-wkhtmltopdf==3.4.0

# dev only (wired into INSTALLED_APPS/MIDDLEWARE/urls solely when DEBUG)
django-debug-toolbar==6.3.0

# --- ops / deployment ---
django-environ==0.13.0      # env-driven settings (SECRET_KEY, DB_*, ...)
psycopg[binary]>=3.2,<4     # PostgreSQL driver (prod; sqlite needs nothing)
sentry-sdk==2.20.0          # opt-in error tracking (enabled when SENTRY_DSN set)
